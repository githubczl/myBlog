name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main  # 或 master，看你实际用的主分支

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: withastro/action@v2
        with:
          site: https://githubczl.github.io/myBlog/